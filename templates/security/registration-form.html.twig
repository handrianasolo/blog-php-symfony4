{% include "partials/header.html.twig" %}

        <main role="main" id="main">
            <!-- Registration's form -->
            <div class="form">
                    <h1>{{ form_title }}</h1>
                    {{ form(form_register) }}
            </div>
        </main>

