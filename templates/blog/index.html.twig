{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="../assets/dist/css/bootstrap.css">
    <link rel="stylesheet" href="../css/style.css">
{% endblock %}

{% block body %}

<div class="album py-5 bg-light">
    <div class="container">
        <div class="row">
            <!-- All articles --> 
                {% for article in articles %}
                    <div class="col-md-4">
                        <div class="card mb-4 shadow-sm">
                            <div class="card-header">
                                {{ article.title }}    
                            </div>
                            <img class="card-img-top" src="{{ article.image }}" alt="A generic placeholder image in a figure.">
                            <div class="card-body">
                                <h6 class="card-subtitle mb-2 text-muted"> Written by {{ article.user.username }}</h6>
                                <p class="card-text">{{ article.content }}</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <p>
<<<<<<< HEAD
                                        <a class="btn btn-secondary my-2" href="{{ path('blog_article_page', {'title': article.title}) }}" 
                                            role="button">View &raquo;</a>
                                        </a>
                                    </p>
                                    <small class="text-muted">{{ article.createdAt | date('d/m/Y') }} | {{ article.createdAt | date('H:i') }}</small>
=======
                                        <a class="btn btn-secondary my-2" href="{{ path('blog_article_page', {'id': article.id}) }}" 
                                            role="button">View &raquo;</a>
                                        </a>
                                    </p>
                                    <small class="text-muted">{{ article.createdAt | date('d/m/Y') }}</small>
>>>>>>> d7cdad896d6bd18b72581a21e96b3951c3144990
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
        </div>
        {{ knp_pagination_render(articles) }}
    </div>
</div>

{% endblock %}
